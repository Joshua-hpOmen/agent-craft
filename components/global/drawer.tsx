import React from 'react'

type Props = {
    onOpen: React.FC,
    children: React.ReactNode,
    title: string
}

const AppDrawer = (props: Props) => {
  return (
    <div>

    </div>
  )
}

export default AppDrawer